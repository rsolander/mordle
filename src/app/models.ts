export interface CharInfo {
    letter: string;
    display: number;
}

export interface EntryInfo {
    ans: string;
    guess: string;
}

export interface APIResponse {
    id: number;
    word: string;
}